# Templates

Files installed into target repositories by `steward init`. This is a root directory, not a workspace package.

Content arrives with later milestones (M03 onward); nothing is installed today.
